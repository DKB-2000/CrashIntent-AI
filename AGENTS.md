# AGENTS.md

이 폴더에서 작업을 시작하기 전에 **`CODEX_HANDOFF.md`를 먼저 전체 읽어라.** 프로젝트 배경, 지금까지 완료된 것,
다음에 할 일(우선순위 순), 반드시 지켜야 할 제약이 전부 정리돼 있다.

그다음 `CODEX_HANDOFF.md`의 "§1. 먼저 읽어야 할 문서" 순서대로 `docs/dacon-236753-대회안내.md` → `README.md` →
`DATA_SOURCES.md` → `docs/domain-knowledge-*.md`를 읽는다.

이 폴더 밖(상위 폴더)으로 올라갈 필요는 없다 — 필요한 문서는 전부 이 폴더 안에 있다.

## 장시간 작업 감시 — Codex 사용량 절약 (2026-09-11 사용자 요청)

- 학습·변환·원격 실행 완료를 기다리기 위해 Codex가 `sleep` → 상태 조회를 반복하지 않는다.
  반복 확인·결과 회수·기계적 검증은 로컬 Python watcher 또는 Windows 예약 작업에 맡긴다.
- 기존 watcher/예약과 상태 파일을 먼저 한 번 확인한다. 이미 실행 중이면 중복 실행하지 않는다.
- 새 장시간 작업은 승인된 범위에서 완료·실패 상태, 로그, 유한 재시도·시간 제한,
  필요한 결과 회수·검증을 남기는 감시기에 연결한다. 상태 파일만 보고 실제 실행을 단정하지 않는다.
- 감시 연결과 최초 시작을 확인한 뒤 독립적으로 할 수 있는 작업을 진행한다. 남은 일이 대기뿐이면
  현재 상태·결과 경로·다음 확인 방법을 알려주고 응답을 마친다. 완료되지 않은 작업을 완료로 보고하지 않는다.
- 사용자가 진행 상황을 물으면 로컬 상태를 한 번 조회한다. 완료 이벤트 또는 사용자의 후속 요청 때
  결과와 검증 증거를 분석한다. 별도 연결 없이 Codex가 자동 재개하거나 알림을 보낸다고 약속하지 않는다.
- 기존 로컬 CPU 감시 간격과 Stage1 ZIP 12시간 예약은 유지한다. 모델·보안·승인 설정 변경은 이 규칙의 범위가 아니다.
- 현재 작업의 한 번 조회: `./scripts/Get-BackgroundWorkStatus.ps1`. 이 명령은 네트워크/API를 호출하지 않는다.
