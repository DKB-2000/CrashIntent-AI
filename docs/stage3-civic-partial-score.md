# Civic 부분 라벨 기존 모델 채점 (2026-09-14)

## 완료 결과

COMPLETE_VALIDATED,감시COMPLETE. 4영상2400시점추론/714시점채점 완료.
ZIP·가중치·AST,원본해시,짝수프레임 무손실픽셀대조,저장로짓재현,
CSV혼동행렬/F1독립검산과 예측CSV SHA256 재확인PASS.

전체 선택집합 조향Macro-F1 **0.820299**. 좌회전재현454/458=99.13%,
직진65/99=65.66%,우회전119/157=75.80%.
05-01 경로286시점F1 .974208,06-11 경로428시점F1 .692889로 경로 차이가 크다.
06-11은 직진21/51=41.18%,우62/100=62%가 남은 약점이다.
총오답76개 중72개가06-11경로에 있다.

기존모델 자체를 다른차량의 선택된안정구간에 평가한 결과이며 모델개선 비교가 아니다.
전체2400중70.25%제외,센서합의 부분라벨·2경로이므로 공식점수나 전체자연주행성능과
직접 비교하지 않는다. 기존모델유지. 다음은06-11 직진·우회전 오류구간 진단이다.
아래착수기록보다완료결과를우선하며 재실행하지 않는다.

사용자 승인으로 고정 기존 모델의 평가2경로4영상 채점을 시작했다.
상태는 `artifacts/stage3-civic-partial-score-20260914/status.json`을 따른다.
모델은 `stage3-motion-submit-candidate-20260910/submit.zip`에 담긴 기존1/5/15 움직임MLP다.
ZIP·가중치 해시와 로컬 Stage3 추론 함수/클래스 AST를 실제 배포 코드와 대조한다.

## 입력과 채점

raw HEVC 전체를 읽어 짝수 프레임만 유지하고 10fps FFV1 AVI로 무손실 저장한다.
각 영상의 유지600프레임을 원본 재디코딩 픽셀과 전부 비교한 뒤 기존 배포 특징추출기를
그대로 사용한다. 파일별로 과거1/5/15 움직임을 구성하고 모든2400시점을 추론한다.
전처리나 모델에 센서값·다른영상의 예측을 넣지 않는다.

사전 고정된 부분 라벨의714시점(직진99/좌458/우157)만 채점하며 UNKNOWN1686시점,
제외70.25%를 함께 보고한다. 고정3클래스LEFT/STRAIGHT/RIGHT Macro-F1과
방향별 재현율,경로별·영상별 결과를 저장한다. 해당 시점들은21개 연속구간으로 서로 상관된다.

저장특징을 다시 읽어 로짓을 exact 재현하고, 별도 표준CSV 파서로 혼동행렬과F1을
검산하며 라벨해시가 그대로인지 확인한다. 공식점수나 사람정답에 대한 성능이 아닌
센서합의·안정구간 선택집합의 진단이다. 제외한 어려운 구간의 성능으로 확장할 수 없다.

## 실행

`src/score_stage3_civic_partial.py watch`: OS중복잠금/30분제한/재시도0,
시간초과시 자식트리 종료,학습없음. 기존 모델·라벨·제출ZIP은 유지한다.
감시 시작 뒤 Codex는 반복대기·폴링하지 않는다.

산출물은 `artifacts/stage3-civic-partial-score-20260914/`의
`plan.json`, `status.json`, `watch-status.json`, `run.log`, 완료시 `report.json`,
`predictions.csv`, 영상별특징/로짓NPZ,무손실AVI다.
상태 한 번 조회는 `scripts/Get-BackgroundWorkStatus.ps1`의 Stage3CivicPartialScore다.
