# Stage2 제출87243 하락 원인 분석 (2026-09-11)

> 현재 상태 (2026-09-11): 분석 완료. 원인 후보와 확인된 사실을 구분하며, 다음 순서는 [진행 계획](stage2-next-steps.md)을 따른다.

## 결론

공식 사용자 보고 Stage2 .2096390642→.1966181317,
차이-.0130209325(약6.21% 상대 하락). 원인 단독 확정은 불가하다.
현재 가장 강한 근거는 학습/미학습 출처 일반화 격차이며, 데이터 분포·라벨 proxy 차이와
검증 결과를 최종 제출 모델로 옮기는 과정의 검증 부족이 관련 가설이다.
비공개 항목별 정답·점수는 없어 어느 항목이 공식 하락을 주도했는지 알 수 없다.

## 파일·코드 검사

로컬 결합 후보 f94995a9b6303fde41b65153372ad9e48d220e5034d86c9e1c05cfea04fe45fb의
Stage2 가중치5f38f4db898e9f71b8220d147b63a7fc8de4eafb94fa933387841cd3ac6c1a43은
생성한 최종 사전학습 모델과 일치한다. 이전 Stage2는 ef5746c...였다.
ResNet 가중치 바이트 동일. 기존 Stage3 motion ZIP과 결합ZIP의 함수·클래스 AST 비교에서
달라진 기존 노드는 s3_motion_features와s3_motion_load뿐,Stage2 코드 불변.
CPU 패키지 클래스 출력66개 일치 검사도 이미PASS였다.
서버 업로드 해시는 미확인으로,제출87243과 로컬 결합후보 연결은 대화기록 근거다.

## 학습/미학습 출처 격차 직접 재계산

| fold | pretrained 학습영상 네 항목 평균 | 미학습 출처 평균 |
|---|---:|---:|
| 0 | .9952 | .5357 |
| 1 | .9717 | .5000 |
| 2 | .9953 | .6346 |
| 3 | .9906 | .4038 |
| 4 | .9764 | .4231 |

최종 전체66영상 모델의 학습자료 재평가는 충돌100%,진입98.5%,회피98.5%,방향98.5%다.
이는 독립 검증이 아니며 학습자료 암기와 일치하는 강한 일반화 격차를 보여준다.
서버 성능 하락의 단독 인과 증명은 아니다. 최종 모델 자체에 별도 미학습 검증은 없다.

## 채택 근거의 한계

사전학습 대조 .4318→.5000은 같은5분할에서 새로 학습한 scratch와 비교한 값이다.
공식 .209639를 달성한 기존 제출 모델과의 동일한 새 독립자료 직접 비교가 아니다.
학습 중앙값·다수클래스 기준선 .4773 대비 개선 모델 .5000은2.27%p 차이였다.
전체66영상·23출처·한seed이며 이미 다른 설정 비교에 사용한 개발표본이다.
최종 모델은검증시 약52~53영상 대신66영상전체15에폭/990업데이트 학습했으며
별도 최종검증 없이 교차검증의 개선을 최종 제출 후보로 옮겼다.
이 절차는 일반적인 full-fit이지만 여기서는 안정적인 공식개선 근거가 부족했다.

CCD 충돌 라벨은 binlabels 최초1의 onset proxy다. 대회 충돌 판정 의미 및 촬영분포와
차이가 있을 가능성이 있으나 이번 분석으로 그 크기나 인과를 확정하지 않는다.
원본 데이터 확대1296영상은 충돌 라벨만 추가했고 진입·회피·방향 정답66개는 그대로다.

## 실행 정밀도 관련 미확인 사항

제출 predict_stage2의 ResNet 특징 추출은 CUDA FP16 autocast, temporal은FP32다.
개발 캐시는 CPU FP32 특징이었다. 기존/신규 모두 추론코드는 같지만 새 가중치가 이 차이에
얼마나 민감한지는 새 GPU 동등성 검사 없이 배제할 수 없다. 정밀도 오류로 확정하지 않는다.

## 조치 제안

당장 성능 기준은 이전 공식Stage2 .209639 모델을 유지하는 것이 타당하다.
이번 분석에서 ZIP·모델·selected를 변경하거나 새학습·GPU·제출을 실행하지 않았다.
다음 검증은 기존/신규 모델을 동일 GPU 특징으로 대조하고,학습출처와 분리된 새 검증자료에서
기존실제제출모델과 비교해야 한다. 이어 최종학습량·조기종료를 내부출처검증으로 결정한다.
기존5분할 평가값을 보고 에폭을 반복선택하면 계속 개발셋에 맞추게 된다는 한계를 명시한다.

근거 artifacts/stage2-drop-analysis-20260911/audit.json.
재현 artifacts/stage2-drop-audit.py. 교차검증 각 모델을 원래 학습행에서 직접 재추론했다.
