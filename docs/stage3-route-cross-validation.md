# Stage3 경로 교차검증 (사람 라벨 없이 진행)

2026-09-11 사용자 요청으로 사람 검수는 보류하고 기존 영상·센서 대리 라벨을 이용한 경로 교차검증을 수행한다. 사람 검수 도구 및 자료는 보존한다.

기존 학습에서 선택한1000클립/17경로를 고정seed20260911로5분할한다. 각검증분할의경로 전체를학습과평균/표준편차 계산에서 제외한다. 이전개발검증4경로31영상은이번학습/평가에쓰지않는다. 경로별 표본수에따라검증크기231/245/173/176/175개다.

기존시간창1/5/15와short1/3/5,3초기값20260910/11/12,총30모델. 동일MLP1682→128→64와2heads,AdamW lr.001/weight_decay0/gradientclip1/고정1200업데이트/배치10. 검증결과로epoch·seed·fold를선택하지않는다. CPU2threads로학습한다.

분할 후 라벨그룹 수가 서로달라 기존동일크기그룹 순회함수를직접쓸수없다. 따라서학습집합내10개라벨조합에서각각1개씩복원추출하는고정배치1200개를미리만들어같은분할의두시간창·세초기값에공통사용한다. 양쪽의조건을동일하게하지만원래전체1000개학습과완전히같은배치프로토콜은아니다.

모든표본은자신의경로를보지않은모델의예측으로만합산한다. 조향F1,클래스평균재현율,직진·좌회전·우회전재현율을모두보고,경로별차이를분리한다. 가감속은보조지표다. 공식Stage3는조향F1이므로가감속개선을공식개선으로환산하지않는다.

한계:1000클립은이전센서라벨기준으로균형선정된표본으로자연분포가아니다. 데이터셋·차량·고속도로환경은기존comma2k19와같으며독립적인새실세계자료가아니다. 원본17경로는기존최종모델이학습한자료지만각교차검증모델은해당평가경로를학습하지않는다. 이번분석을사람검수나공식점수의대체로주장하지않는다.

실행 src/compare_stage3_route_cv.py prepare/train/verify. 결과 artifacts/stage3-route-cv-20260911/. 검증조건·코드/입력해시 plan.json,진행status.json,최종 summary.json 및independent-validation.json. 검증은모든30checkpoint 재로딩·정규화·경로분리·원본캐시해시·저장배치의10그룹균형·모든검증로짓완전일치·1000개OOF완전커버를확인한다.

현재 모델·제출ZIP·selected.json·다른Stage작업은변경하지않는다. 신규데이터다운로드/GPU업로드/사람라벨생성없음.

## 완료 결과 (2026-09-11)

30모델학습372.219초,전모델재로딩·경로분리·train-only정규화·배치·검증로짓 재현오차0 PASS. 원본표본과OOF CSV를별도csv모듈로읽어조향혼동행렬도다시확인했다. independent-validation.json PASS.

| 표본별 미학습경로 예측 합산,3seed평균 | 기존 | short |
|---|---:|---:|
| 조향F1 | .718900 | .709580 |
| 가감속F1 | .497810 | .496787 |
| LEFT재현율 | 75.44% | 75.56% |
| STRAIGHT재현율 | 65.00% | 60.56% |
| RIGHT재현율 | 74.78% | 76.67% |

조향F1은3seed각각 .724239→.708892, .719217→.708203, .713245→.711646으로모두하락했다. 5분할평균4개하락/1개상승,분할×seed15쌍중10쌍하락/5쌍상승. 17경로중12경로평균하락/5경로상승이다. 표본과모델학습이서로겹치므로이를15개의완전히독립된통계실험으로해석하지않는다.

이전4경로검증에서는short직진재현율상승·우회전하락이었지만,이번17경로교차검증에서는직진하락·우회전상승이다. 따라서short가항상직진으로편향된다는일반원인을주장할수없다. 이전실제제출모델에서관찰한회전→직진손실은해당모델·검증분포의관찰로유효하지만,시간창단축의보편적효과는아니다. 더욱확실해진것은짧은창의이득이학습/평가경로에따라바뀌며일관된일반화개선이아니라는점이다.

결론:short를다음제출개선안으로승격하지않고기존1/5/15시간창모델유지. 사람검수없이도이를확인하는추가근거를확보했다. 추후개선은기존시간문맥을유지한채새특징/학습방식을경로분리조건에서검증하는방향으로잡고,이번기존캐시CV결과를새실세계성능으로환산하지않는다. 기존공식최고ZIP/selected/다른Stage설정변경없음.
