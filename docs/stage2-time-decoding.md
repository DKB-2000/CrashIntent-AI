# Stage2 1번: 시점 선택 대조 완료 (2026-09-10)

> 현재 상태 (2026-09-11): 완료·검증 PASS, 제출 미반영. 아래 다음 단계 승인 대기는 당시 기록이며 [최신 진행 계획](stage2-next-steps.md)을 우선한다.

사용자가 1,2,3 순차 진행하되 다음 번호 전 승인을 요구했다.
이번에는 1번만 완료했다. 2번 분류 특징 개선 및3번 사전학습은 미착수, 승인 대기다.

src/evaluate_stage2_time_decoding.py를 CPU 실행했다. 기존5분할66영상23출처,
3학습 설정의 저장 모델을 고정하고 argmax / 충돌만 확률합 / 진입만 확률합 /
둘 다 확률합을 비교했다. 확률합 범위는 사전 고정 ±0.3초, 경계는 유효 프레임만 합산한다.
기존 scene head를 선택된 두 시점에서 다시 평가했으며 구조·학습·주변구간 pooling은 바꾸지 않았다.
Stage1 GPU 작업 및 현재 제출 모델·ZIP 변경 없음.

| 교차검증 학습 설정 | 기존 argmax 개발평균 | 두 시점 확률합 | 변화 |
|---|---:|---:|---:|
| exact5 | .4280 | .4432 | +.0152 |
| exact15 | .4015 | .4356 | +.0341 |
| soft_mixed15 | .4508 | .4470 | -.0038 |

soft_mixed15에서 충돌만 변경은 .4508로 동일, 진입만 변경은 .4470이다.
진입 허용오차 내 정답은21/66→22/66이나 회피33→32,방향32→31로 줄어 전체 평균이 하락했다.
exact5의 영상 평균은 개선됐지만 출처 동일가중 평균은 .4252→.4201이다.
현재 개선 학습 설정에 시점 확률합을 일괄 적용할 근거가 부족하여 제출 코드에는 반영하지 않는다.
기존 교차검증 모델로 탐색한 개발 결과이며 현재 공식 제출 가중치의 새 공식 점수가 아니다.

출력 artifacts/stage2-time-decoding-20260910/report.json COMPLETED, validation PASS.
모든 기존 argmax 예측이 저장 OOF와 일치했고 출처 분리·66영상 포함·입력 해시를 확인했다.
창 합산은 독립적인 시간차 기반 직접 합산과 대조했고, 경계와29.97FPS 검사도 통과했다.
전체 지표는 영상별 정오표 평균과 교차 확인했다. 모델별4종 상세 예측 CSV와 체크포인트 해시 보존.

다음 제안: 2번 회피·방향 분류의 전후 구간 특징 대조. 사용자 승인 전 실행하지 않는다.
