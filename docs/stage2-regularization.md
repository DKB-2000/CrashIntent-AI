# Stage2 학습량·인코더 고정 대조 (2026-09-11)

> 현재 상태 (2026-09-11): 완료·독립 검증 PASS. BiGRU 고정은 후속 후보이며 내부 설정 선택 절차는 기존 기준을 넘지 못했다.

사용자가 새 개선방안의1번부터 진행하도록 승인했다. 현재CPU 실행 중이다.
기존 공식 최고Stage2 및 제출ZIP은 변경하지 않는다. 새외부업로드/GPU실행 없음.

## 사전 고정 프로토콜

기존66영상/23출처의5개 외부fold를 유지한다. 외부fold별 나머지 학습출처 중
약20%(최소2출처)를 seed20260911+fold로 내부검증에 배정한다.
내부학습/내부검증/외부평가 세 출처집합이 완전히 분리되는지 검사한다.
1296영상 충돌사전학습 모델은 기존수동23출처 전체를 제외한 것으로 공유한다.

- early_stop: BiGRU/head lr2e-4, 내부검증으로1~15에폭 중 종료시점 선택.
- low_encoder_lr: BiGRU2e-5, head2e-4, 내부검증으로 종료시점 선택.
- frozen_encoder: BiGRU 고정·eval(dropout비활성), head2e-4, 같은 선택 방식.

동일초기화,seed20260825,영상순서·scene혼합선택 RNG분리,soft_mixed손실,
AdamW기본설정·gradient clip1 유지. frozen의 dropout변화도 대조조건에 포함됨을 명시한다.
내부검증 출처동일가중 네항목 평균 최대,동률시가장 이른에폭을 선택한다.
각설정의 에폭과 전체 설정 선택도 내부점수만 사용하며 동률시위기재순서를 따른다.
이 결정을 selection.json에 고정한 뒤 각설정을 외부학습전체에서 선택에폭만큼재학습한다.
외부평가는 최종재학습 이후에만 수행한다. 각조건 OOF 및 내부선택조건 OOF를 따로보고한다.
에폭전이는 내부학습영상수보다 외부학습영상수가 많다는 차이를 포함한다.

기존외부fold는이미개발에사용돼왔으므로 새로운독립테스트가 아니다.
한seed의탐색적대조이며공식점수상승을보장하지 않는다.
기존15에폭pretrained결과는참고로남기고,외부결과를보고최적에폭을고르지 않는다.

## 실행

src/run_stage2_regularization.py CPU2스레드 숨김PID13012.
artifacts/stage2-regularization-20260911/status.json에분할/단계/조건/에폭기록.
로그 artifacts/stage2-regularization-20260911.log 및-error.log.
초기6에폭52초로전체약30~60분예상이나선택에폭/CPU부하에따라변동한다.

src/validate_stage2_regularization.py 숨김PID36140이최대12시간결과를기다린후
내부분할분리·내부저장예측에서점수독립재계산·최초최적에폭/설정선택재검산,
외부15모델재로딩전예측재현·고정BiGRU텐서불변·66OOF완전성·집계지표를검사한다.
report COMPLETED 및 independent-validation PASS 모두확인후완료로보고한다.
중복실행금지,로컬PC가켜져있어야계속진행된다.

## 완료 확인 (2026-09-11)

학습1809.187초(약30분9초),report COMPLETED 및 independent-validation PASS.
15개 외부모델 예측재현,내부점수·에폭·설정선택재계산,3분할출처분리,
고정인코더텐서불변,66영상OOF완전성 및집계지표 재검산PASS.

| 설정 | 영상별 네항목 평균 | 출처동일가중 평균 |
|---|---:|---:|
| 이전15에폭 사전학습 기준 | .5000 | .5156 |
| 조기종료 | .4659 | .5103 |
| BiGRU 저학습률 | .5114 | .5152 |
| BiGRU 고정 | .5227 | .5248 |
| 내부검증으로 조건까지선택 | .4848 | .4982 |

고정조건은 기존기준대비영상평균+2.27%p이나작은개발결과이며외부결과를보고고른최고조건이다.
사전정의한내부조건선택절차는48.48%로기준50%를넘지못했다.
따라서안정적인개선이나공식점수상승으로해석하지않고,고정조건은후속검증후보로만남긴다.
새ZIP/최종모델교체/공식제출없음. 이후개선은사용자와결정한다.
