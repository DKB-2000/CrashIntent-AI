# 프로젝트 작업 로그

이 문서는 작업의 목표, 진행 과정, 결과, 배운 점을 다음 작업자가 빠르게 파악할 수 있도록 기록한다.
터미널 출력이나 대화 전문이 아니라 재현에 필요한 결정과 검증 결과를 중심으로 유지한다.

## 2026-09-02 — 프로젝트 분석 및 Stage 2 데이터 준비

### 목표

- 데이콘 236753 대회의 3단계 문제 구조와 제출 규격을 파악한다.
- Stage 2 학습 데이터 후보를 조사하고 CCD 데이터를 확보한다.
- CCD 영상에 수동 라벨을 입력할 수 있는 도구를 준비한다.

### 과정

- 공식 대회 안내와 베이스라인 노트북을 분석해 Stage별 입출력과 평가 방식을 문서화했다.
- CCD와 DoTA를 조사하고 실제 파일과 라벨 스키마를 검증했다.
- OpenCV 기반 Stage 2 수동 라벨링 GUI인 `src/label_stage2.py`를 구현했다.
- 로컬 Python 3.11 가상환경에서 영상 표시, 키 입력, CSV 저장 및 재개 동작을 확인했다.
- Kaggle Tesla T4에서 공식 학습·추론 노트북과 제출 ZIP 생성 과정을 검증했다.

### 결과

- CCD 사고 영상 1,500건과 공식 `collision_frame` 라벨을 확보했다.
- `entry_frame`, `evasion_space`, `entry_side`는 CCD에서 제공되지 않아 수동 라벨링이 필요하다고 확정했다.
- DoTA는 필요한 Stage 2 라벨의 직접적인 대체 데이터로 부적합하다고 판단했다.
- Stage 2 라벨링 도구와 Phase 0 베이스라인 검증을 완료했다.

### 배운 점

- 데이터셋 이름이나 논문 설명만으로 라벨 적합성을 판단할 수 없으며 실제 스키마와 영상을 함께 확인해야 한다.
- Kaggle에서 requirements 설치 후 NumPy/Pandas ABI 충돌이 발생할 수 있어 세션 재시작이 필요하다.
- 사람이 만든 라벨은 원본 영상보다 용량은 작지만 복구 비용이 크므로 별도 백업이 중요하다.

## 2026-09-03 — Stage 3 데이터 변환 검증

### 목표

- comma2k19가 Stage 3 가감속·조향 학습 데이터로 사용 가능한지 확인한다.
- 영상과 차량 센서 신호를 10Hz 학습 샘플로 변환하는 파이프라인을 만든다.

### 과정

- comma2k19 공식 1분 예제를 내려받아 영상, 속도, 조향, 가속도 timestamp를 비교했다.
- 20Hz 영상 1,200프레임을 목표 10Hz 600개 샘플로 변환했다.
- 속도와 조향 신호를 timestamp 기준으로 정렬하고 smoothing 및 threshold를 적용했다.
- `src/prepare_stage3_comma2k19.py`에서 학습 CSV, 디버그 CSV, 분포 JSON 및 오버레이 영상을 생성하도록 구현했다.
- Kaggle에서 변환 결과와 육안 오버레이를 확인했다.

### 결과

- Stage 3 변환 런타임 검증이 PASS했다.
- 600개 샘플의 분포는 가감속 `158/285/157`, 조향 `356/139/105`로 생성됐다.
- 현재 범위에서는 comma2k19 1분 예제로 데이터·변환 타당성 검증을 완료한 것으로 처리했다.
- 대용량 Stage 3 데이터 확대는 보류했다.

### 배운 점

- 영상 FPS와 센서 측정 주기가 다르므로 프레임 번호가 아닌 timestamp로 동기화해야 한다.
- 자동 생성 라벨은 분포 확인만으로 충분하지 않고 오버레이를 통한 육안 검증이 필요하다.
- 센서 축 방향과 조향 부호는 데이터셋 정의를 확인하고 명시적인 라벨 매핑으로 남겨야 한다.

## 2026-09-03 — Stage 1 합성 및 모델 파이프라인 검증

### 목표

- 원본 영상에 화면 재촬영 특성을 합성해 Stage 1 학습 데이터를 생성한다.
- 합성 데이터로 학습부터 체크포인트 저장, 추론 CSV 생성까지 전체 흐름을 검증한다.

### 과정

- 모아레, 픽셀 격자, 감마·색 변화, 블러, 원근 변화 등 재촬영 특성을 적용하는 `src/prepare_stage1_rerecorded.py`를 구현했다.
- 원본 source ID 단위로 train/validation을 나눠 파생 영상이 서로 다른 split에 섞이지 않도록 했다.
- `src/stage1_pipeline.py`를 이용해 MViTv2-S 기반 학습 및 추론 스모크 테스트를 실행했다.
- 공식 원본 2개로 ORIGINAL 2개와 RERECORDED 4개를 생성하고 결과를 육안 확인했다.
- Kaggle CUDA에서 2 samples, 1 epoch 학습과 체크포인트 재로딩, `ID,answer` 출력 계약을 확인했다.

### 결과

- 합성 데이터 생성 검증이 PASS했고 source leakage가 없음을 확인했다.
- Stage 1 모델 스모크 테스트가 PASS했다.
- 실제 일반화 성능 검증을 위해 학습에서 제외한 원본 30개를 휴대전화로 3조건씩 재촬영해 약 90개 검증 세트를 만들기로 했다.

### 배운 점

- 합성 데이터의 학습 정확도는 실제 모니터 재촬영 영상에 대한 일반화 성능을 의미하지 않는다.
- 같은 원본에서 만든 파생 데이터는 반드시 source 단위로 같은 split에 배치해야 정보 누출을 막을 수 있다.
- Stage 1의 가장 큰 위험은 모델 구현보다 합성 영상과 실제 재촬영 영상 사이의 domain gap이다.

## 2026-09-03 — 독립 Git 저장소 및 경로 이식성 준비

### 목표

- 프로젝트를 독립 Git 저장소로 만들고 어떤 경로에 clone해도 실행할 수 있게 한다.
- 장비마다 달라지는 데이터 경로를 소스코드에서 분리한다.
- 대용량 데이터와 모델이 실수로 Git에 포함되지 않게 한다.

### 과정

- 프로젝트 루트에 `main` 브랜치의 독립 Git 저장소를 초기화했다.
- 기본 상대경로를 `project.properties`에 정의했다.
- 장비별 override 예시로 `project.local.properties.example`을 추가했다.
- `src/project_config.py`에서 기본 설정과 로컬 override를 읽고 상대경로를 프로젝트 루트 기준으로 해석하도록 했다.
- Stage 2 라벨링 도구가 하드코딩된 위치 대신 properties 설정을 사용하도록 변경했다.
- `.gitignore`와 `.gitattributes`를 보강하고 `docs/git-portability.md`에 clone 및 게시 절차를 기록했다.

### 결과

- 소스와 문서는 Git staging 상태까지 준비됐다.
- `data_raw/`, 가상환경, 대용량 ZIP, 모델 체크포인트, 로그 및 로컬 properties는 Git 추적에서 제외했다.
- 원격 저장소 URL이 아직 없어 commit과 push는 수행하지 않았다.

### 배운 점

- 저장소 이식성은 프로젝트 루트의 절대경로를 없애는 것뿐 아니라 데이터·모델·실행환경의 복원 방법까지 문서화해야 확보된다.
- 대용량 원본 데이터와 학습 모델은 Git보다 ZIP, 외장 저장장치, Kaggle Dataset 또는 별도 아티팩트 저장소가 적합하다.
- `project.local.properties`처럼 장비 종속 설정은 예시만 추적하고 실제 값은 ignore해야 한다.

## 다음 작업

1. Git staging 목록과 대용량·민감 파일 포함 여부를 최종 점검한다.
2. 원격 Git 저장소 주소를 등록하고 초기 commit 및 push를 수행한다.
3. CCD 전체를 source ID 기준으로 분할해 Stage 1 실제 합성 학습 세트를 생성한다.
4. 학습에서 제외한 원본 30개로 휴대전화 재촬영 검증 세트를 만든다.
5. Stage 2 CCD 200~300건을 우선 수동 라벨링한다.
6. 로컬 평가 하네스와 제출 컴플라이언스 검사를 구현한다.

## 기록 규칙

- 중요한 작업이 끝날 때마다 날짜별 항목을 추가한다.
- 각 항목에는 반드시 목표, 과정, 결과, 배운 점을 기록한다.
- 원본 데이터 출처와 라이선스 변경은 `DATA_SOURCES.md`에도 함께 반영한다.
- 상세 실행법은 해당 runbook에 기록하고 이 문서에는 결정과 검증 결과를 요약한다.

## 2026-09-03 — Stage 1 전체 생성용 holdout 보호 기능

### 목표

- 휴대전화 재촬영 검증용 CCD 원본 30개가 합성 학습 데이터에 들어가지 않도록 재현 가능한 제외 절차를 만든다.

### 과정

- `src/select_stage1_holdout.py`를 추가해 seed와 source ID 해시로 holdout을 결정하고 CSV로 저장하게 했다.
- `src/prepare_stage1_rerecorded.py`에 `--exclude-file` 옵션을 추가했다.
- 제외 CSV의 `source_id`가 입력에 없거나 중복 source stem이 존재하면 즉시 실패하도록 검증을 추가했다.
- 공식 예제 5개에서 holdout 1개를 선택해 CSV 생성까지 실행했다.

### 결과

- 정적 계약 검사와 Python 문법 검사가 PASS했다.
- 동일 seed에서 공식 예제 `000004`가 holdout으로 선택됐다.
- 로컬 전체 렌더링 검사는 `ffmpeg`가 PATH에 없어 인코딩 시작 전에 중단됐다. 기존 Kaggle 환경에서는 생성기 런타임이 이미 PASS했으며 새 제외 옵션의 전체 Kaggle 검증이 남았다.

### 배운 점

- 실제 촬영 전에 원본 ID를 고정하고 CSV를 보존해야 실수로 학습 데이터와 섞이는 것을 예방할 수 있다.
- holdout 선택은 파일 나열 순서가 아니라 seed와 source ID에만 의존해야 다른 장비에서도 동일하게 재현된다.

## 2026-09-03 — Nexar Stage 1 외부 데이터 후보 검증

### 목표

- 최초 CCD 프로토타입 이후 Stage 1 일반화 성능을 개선할 외부 대시캠 source 후보를 검증한다.

### 과정

- Nexar 공식 Hugging Face 저장소와 Nexar Open Data License 전문을 확인했다.
- 전체 31.4GB 대신 train positive 2개와 negative 2개를 Kaggle에 개별 다운로드했다.
- 네 영상을 직접 재생해 전방 대시캠 영상 여부와 품질을 확인하고, 각각 ORIGINAL 및 합성 RERECORDED 한 개씩 생성했다.

### 결과

- 합성 생성 명령이 return code 0으로 완료됐다.
- 사용자가 영상과 합성 품질을 확인하고 Nexar를 Stage 1 추가학습 데이터 후보로 채택했다.
- 최초 프로토타입 전에는 전체 다운로드하지 않고, 프로토 결과 확인 후 50개 확장 검수와 전체 도입을 진행한다.

### 배운 점

- Nexar의 positive/negative는 사고 유무 라벨이며 Stage 1의 ORIGINAL/RERECORDED와 무관하다.
- 외부 데이터셋의 두 그룹 모두 동일하게 ORIGINAL과 합성 RERECORDED 쌍을 만들어야 데이터셋 출처가 정답 지름길이 되는 것을 막을 수 있다.
