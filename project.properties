# Portable defaults. Relative paths are resolved from project.root.
# Keep project.root=. so the repository can be cloned to any directory.
project.root=.

data.raw.dir=data_raw
data.working.dir=data
artifacts.dir=artifacts
baseline.dir=Baseline

ccd.label.file=data_raw/ccd/Crash-1500.txt
ccd.video.dir=data_raw/ccd/videos/Crash-1500
stage2.manual.labels=data/stage2/labels_manual.csv

# Machine-specific overrides belong in project.local.properties (git-ignored).
